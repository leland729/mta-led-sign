secrets = {
    "ssid": "YOUR_WIFI_SSID",
    "password": "YOUR_WIFI_PASSWORD",
    # Optional: override the default Cloud Run server URL
    # "api_url": "https://subway-api-336mpuaosa-ue.a.run.app",
}
